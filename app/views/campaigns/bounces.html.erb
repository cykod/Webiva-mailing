<div class="admin_content">

<% admin_form_for :bounce, @bounce_manager do |f| -%>
  <%= f.radio_buttons :action, @bounce_manager.class.action_options, :separator => '<br/>' %>
  <%= f.spacer %>
  <%= f.submit_tag 'Submit' %>
<% end -%>

</div>
